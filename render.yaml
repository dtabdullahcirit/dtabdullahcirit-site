services:
  - type: web
    name: dtabdullahcirit-site
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - key: COOKIE_SECURE
        value: true
